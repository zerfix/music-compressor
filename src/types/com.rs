use std::path::PathBuf;

//-////////////////////////////////////////////////////////////////////////////
//  Thread comunication interface
//-////////////////////////////////////////////////////////////////////////////
/// Channel interface for file actions.
pub enum Actions {
    Copy(PathBuf),
    Remove(PathBuf),
    Rename(PathBuf, PathBuf), // from, to
}
//-////////////////////////////////////////////////////////////////////////////
//
//-////////////////////////////////////////////////////////////////////////////
